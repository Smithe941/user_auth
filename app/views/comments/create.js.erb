$('#comments').append('<%= j render @comment, comment: @comment %>');
$('#new_comment textarea').val('');
$('#post_<%= @comment.post.id %> .comment-field').html("<%= @comment.post.comments.count %> Comments")
